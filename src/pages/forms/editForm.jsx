import React from "react";

import FormBuilder from "@/components/forms/formBuilder";

const EditForm = () => {
  return <FormBuilder initialData={{}} onSubmit={() => {}} mode="edit" />;
};

export default EditForm;
