import React from "react";

import FormBuilder from "@/components/forms/formBuilder";

const NewForm = () => {
  return <FormBuilder initialData={{}} onSubmit={() => {}} mode="new" />;
};

export default NewForm;
